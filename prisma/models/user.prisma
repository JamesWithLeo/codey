
model Cart {
  id        String     @id @default(cuid())
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  user_id   String
  user      User      @relation("UserToCarts", fields: [user_id], references: [id])
  cartItems CartItem[]
}

enum Role {
  user
  admin
}
